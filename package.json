{
  "name": "shopify-stripe-checkout",
  "version": "1.0.0",
  "private": true,
  "description": "Stripe Checkout bridge for a Shopify store: replaces Shopify checkout with a Stripe-hosted page and recreates the paid order back in Shopify via the Admin API.",
  "type": "commonjs",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "stripe": "^17.5.0"
  }
}
